{
  "name": "clawr",
  "description": "Clawr Semantic Library",
  "author": "Clawr Community",
  "license": "MIT",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "test:unit": "tsc --noEmit && bun test ./tests/unit/",
    "build": "tsc --noEmit && npm run build:runtime && npm run build:rwrc",
    "build:schema": "rm -rf .typia-temp && ttsc -p tsconfig.typia.json && node .typia-temp/tools/generate-cir-schema.js && npx prettier --write .vscode/settings.json",
    "build:rwrc": "rm -rf .typia-temp && ttsc -p tsconfig.typia.json && bun build .typia-temp/rwrc/index.js --compile --outfile dist/rwrc",
    "build:typia": "rm -rf generated/cir-parser && npx ttsc -p tsconfig.typia.generated.json",
    "test:backend": "npm run build:runtime && npm run build:typia && bun test ./tests/backend/*.test.ts",
    "test:e2e": "npm run build && bun test ./tests/e2e/*.test.ts",
    "build:runtime": "mkdir -p dist && rm -rf dist/include && bun run cp -R src/runtime/include dist/include && clang -I dist/include src/runtime/*.c -dynamiclib -fvisibility=hidden -current_version 0.0.1 -compatibility_version 0.0.1 -o dist/libClawr.A.dylib",
    "test:runtime": "npm run build:runtime && bun test ./tests/runtime/*.test.ts",
    "test": "npm run test:unit && npm run test:backend && npm run test:e2e"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "decimalish": "^0.1.2",
    "typia": "^14.0.4"
  },
  "devDependencies": {
    "@ttsc/paths": "^0.28.4",
    "@types/bun": "^1.4.0",
    "@types/node": "^26.4.1",
    "bun": "^1.4.0",
    "eslint": "^10.9.1",
    "prettier": "^3.9.6",
    "ttsc": "^0.28.4",
    "typescript": "^7.0.2"
  }
}
