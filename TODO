# TODO

- Implement and test lowering from AST to IR
- Implement the parser to generate the AST

  ```clawr
  data DataStructure {
      x: truthvalue
      y: truthvalue
  }

  const a: DataStructure = {
      x: true
      y: ambiguous
  }

  a.y = false
  print a.x
  ```

- Improve lowering of `integer` literals to `Integer*`
- Allow printing `integer` variable (no temp for the literal)
